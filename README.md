# Cars-Django-Project

In this project I tried to copy a demo car sample data from a csv file to database which will only add new data and not duplicates. Also tried to create a form to add data to database (designed in a way that only new dataset is inserted possible) and delete a data set which is already present in database.
